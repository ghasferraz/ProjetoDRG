@model IEnumerable<ProjetoDRG.Models.BancoSistema>

@{
    ViewData["Title"] = "Index";
}

<h2>TELA DE BANCO SISTEMA</h2>

<p>
    <a asp-action="Create">Criar Novo Banco Sistema</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.banco)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.sistema)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.banco.Nome)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.sistema.NomeSistema)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
            </td>
        </tr>
}
    </tbody>
</table>
